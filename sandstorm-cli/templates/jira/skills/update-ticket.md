# Update Ticket

Update a Jira ticket's description (used by the spec-refine workflow).

## Usage

```bash
.sandstorm/scripts/update-ticket.sh <ticket-id> <body>
```

Requires the Atlassian MCP server or a custom REST API implementation.
