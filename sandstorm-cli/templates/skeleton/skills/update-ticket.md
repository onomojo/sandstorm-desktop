# Update Ticket

Update a ticket's body/description (used by the spec-refine workflow).

## Usage

```bash
.sandstorm/scripts/update-ticket.sh <ticket-id> <body>
```

**This script needs to be configured.** Edit `.sandstorm/scripts/update-ticket.sh` and replace the placeholder with your ticket system's API call.
