# Start Ticket

Mark a ticket as started (assign, label, transition to in-progress).

## Usage

```bash
.sandstorm/scripts/start-ticket.sh <ticket-id>
```

**This script needs to be configured.** Edit `.sandstorm/scripts/start-ticket.sh` and replace the placeholder with your ticket system's API call.
